#include <stdio.h>

/**
 * main - print single digits in base 10
 *
 * Return: 0 returned
 */

int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
		printf("%d", x);
	}
	printf("\n");
	return (0);
}
